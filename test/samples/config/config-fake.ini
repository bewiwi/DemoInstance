[DEFAULT]
log_level=DEBUG
security_type=auth_fake
provider=fake

[AUTH_FAKE]
admin:admin

[HTTP]
port=8080

[DATABASE]
connection:mysql://root:password@db:3306/demo

[IMAGE]
desc=no desc
info='image with no info'
time_default=2


[IMAGE_BI]
name=BI2
desc=PSIH Business Intelligence
info=Les identifiants de tests sont :<br />Login dimtim<br />Password dimitim/*-+
img=/instance_image/logo-psih-large.png
#Image id to use
image_id=BI-hosp-maj-dev
#Flavor Id to use
flavor_id=bi.medium
#time in min before destory instance
time_default=20
#Prefix name
prefix=bi_
#url in instance to check if ready %ip% as placeholder
check_url=https://%ip%/cas/login
soft_url=https://%ip%/
max_instance=10
time_max=80

[IMAGE_SUGAR]
name=Sugar
desc=SugarCRM
img=/instance_image/sugar.jpg
info=Les identifiants de tests sont :<br />Login admin<br />Password admin69
#Image id to use
image_id=CirrOS 0.3.2
#Flavor Id to use
flavor_id=m1.tiny
#time in min before destory instance
time_default=5
#Prefix name
prefix=pilot
#url in instance to check if ready %ip% as placeholder
check_url=http://%ip%/
soft_url=http://%ip%/
max_instance=10
user_data:#cloud-config
    password: root/*-+
    chpasswd: { expire: False }
    ssh_pwauth: True

[IMAGE_GITLAB]
name=Gitlab
desc=Gitlab interface
img=/instance_image/gitlab.png
#Image id to use
image_id=CirrOS 0.3.2
#Flavor Id to use
flavor_id=m1.tiny
#time in min before destory instance
time_default=20
#Prefix name
prefix=cirros
#url in instance to check if ready %ip% as placeholder
check_url=http://google.fr/
soft_url=http://%ip%/
max_instance=10
time_max=80

[IMAGE_CIRROS2]
name=Jenkins
img=/instance_image/jenkins.jpg
desc=Jenins interface demonstration
#Image id to use
image_id=CirrOS 0.3.2
#Flavor Id to use
flavor_id=m1.tiny
#time in min before destory instance
time_default=20
#Prefix name
prefix=cirros
#url in instance to check if ready %ip% as placeholder
check_url=http://google.fr/
soft_url=http://%ip%/
max_instance=10
time_max=80

[IMAGE_CIRROS3]
name=CirrOS 0.3.2
desc=demosception test
img=/instance_image/example.png
#Image id to use
image_id=CirrOS 0.3.2
#Flavor Id to use
flavor_id=m1.tiny
#time in min before destory instance
time_default=20
#Prefix name
prefix=cirros
#url in instance to check if ready %ip% as placeholder
check_url=http://google.fr/
soft_url=http://%ip%/
max_instance=10
time_max=80
